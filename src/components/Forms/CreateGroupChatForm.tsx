
//Has no props
//Here create the HTML and CSS for the form
// Also handles the submitting of information (user, name, picture, organization) to "CreateGroupChat" backend function
//Redirects user with targetRoomId returned from the backend function
function CreateGroupChatForm() {

    

    return (
        <div>

        </div>
    )
}

export default CreateGroupChatForm
